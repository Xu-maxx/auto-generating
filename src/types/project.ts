export interface ProjectData {
  id: string;
  name: string;
  style: string; // lighting/coloring style
  createdAt: string;
  updatedAt: string;
  sessionCount: number;
}

export interface ProjectMetadata {
  id: string;
  name: string;
  style: string;
  createdAt: string;
  updatedAt: string;
  sessionCount: number;
} 